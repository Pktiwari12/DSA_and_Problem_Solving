#include<stdio.h>
#include "all_operations.c"
void insert_at_beg_cll(struct node**tail)
{
    struct node*new_node=NULL;
    new_node = (struct node*)malloc(sizeof(struct node));
    if(new_node == NULL)
    {
        printf("DMA is not allocated.");
    }

}
